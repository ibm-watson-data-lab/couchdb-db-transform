/*
 * Sample transformation function that does not manipulate the source document
 * @param {Object} doc - the source document
 * @returns {Object} - the transformed document
 */
module.exports = function(doc) {

	// do nothing
	
	return doc;
};